#!/bin/bash
echo `date` "<%= spec.name %>:<%= spec.index %> stop" >> /var/vcap/sys/log/lifecycle-job/out.log
/sbin/start-stop-daemon \
  --pidfile "/var/vcap/sys/run/lifecycle-job/pid" \
  --remove-pidfile \
  --retry TERM/20/QUIT/1/KILL \
  --oknodo \
  --stop
